# 更新日志

## 1.7.2 - 2026-08-28

### 兼容性

- 基于插件 SDK `1.4.0` 重新构建并将最低宿主版本提升到 23；在完成 V3 迁移并通过迁移、恢复、业务与降级测试前，API1 版本只接受兼容性与数据修复，旧 `shizuku_auth` 依赖由同 ID 的合并插件满足。
- 保持设置、原神记录、星铁记录与米游社会话的 Dataset v1 契约，供后续 Runtime v2 空环境恢复和降级演练。

## 1.7.1（2026-08-21）

### 新增特性

- 正式提供分析设置、原神记录、星铁记录和米游社会话的独立导入、导出和删除。
- 设置与两类抽卡记录支持替换或按主键去重合并；米游社会话明确仅支持替换。

### 安全与可靠性

- 记录替换和删除均在单次 SQLite 事务中按游戏处理，并同步维护账号与卡池完成状态；会话凭据在目标安装中使用 Android Keystore 重新加密。

### 兼容性

- 基于正式插件 SDK `1.3.1` 构建，最低宿主版本提升到 22；本版本是 Bridge 预发布线验证后的正式版本。

## 1.7.0-bridge.5（2026-08-21）

### 优化

- 重新基于插件 SDK `1.3.0-bridge.5` 构建，跟随宿主完整的浅色／深色容器色阶，避免共享界面回退到默认蓝紫色。

### 兼容性

- 最低宿主版本提升到 21。

## 1.7.0-bridge.4（2026-08-21）

### 新增特性

- 导入前分别探测分析设置、原神记录、星铁记录和米游社会话是否已有数据。
- 分析设置与两类抽卡记录支持用户选择替换或合并；替换会在同一 SQLite 事务中先清理目标游戏的记录、账号与同步状态，再写入数据包内容，合并继续按记录主键去重。
- 米游社会话明确为仅支持替换，导入界面会禁用合并。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.4`，最低宿主版本提升到 20。

## 1.7.0-bridge.3（2026-08-20）

### 新增特性

- 数据管理支持分别删除分析设置、原神记录、星铁记录与米游社会话；两类记录按游戏在单次 SQLite 事务中清理。

### 安全与可靠性

- 删除记录时同步清理账号与卡池同步状态并验证目标游戏已无残留账号；会话和设置删除后同样执行状态校验。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.3`，最低宿主版本提升到 19；该版本仅投放 Debug 渠道。

## 1.7.0-bridge.2（2026-08-14）

### 新增特性

- Migration Bridge 支持恢复分析设置、原神与星铁记录以及米游社会话；记录按原有 MERGE 语义导入。

### 安全与可靠性

- 抽卡 Dataset 使用流式 JSON 解析和单次 SQLite 事务，恢复后校验账号、记录摘要和卡池完成状态；会话凭据使用当前安装的 Android Keystore 重新加密。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.2`，最低宿主版本提升到 18；该版本仅投放 Debug 渠道。

## 1.7.0-bridge.1（2026-08-14）

### 新增特性

- Debug 预发布接入只读 Migration Bridge，分别导出原神记录、星铁记录、分析设置与米游社会话。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.1`，最低宿主版本提升到 17；该版本仅投放 Debug 渠道。

## 1.6.1（2026-08-12）

### 新增特性

- 概览、记录、获取、数据四个标签页支持左右滑动切换，与点击标签等价。页内横向滚动的筛选条仍然优先响应，滑到尽头才会翻页。

### 修复

- 稀有度配色补齐深色档位，5 星金、4 星紫、3 星蓝在深色主题下不再发闷；运气档位与 UP／歪改用应用语义色，与其他界面的成功／警告／危险保持一致。

### 优化

- 间距与圆角改用共享设计系统的取值，UP／歪来源说明默认收起。
- 等待授权的提示不再出现内部术语「宿主」。

### 兼容性

- 编译与测试基线同步到 Android Tool Suite 插件 SDK 1.2.0，继承应用选择的主题和默认／动态配色。
- 数据兼容声明扩展为可读 `v0–v1`，可承接旧安装记录和未声明版本保存的数据库、账号选择及分析设置，不再因此阻止重新安装或版本切换。

## 1.6.0（2026-08-07）

### 新增特性

- `main` 分支单元测试和构建成功后自动更新公开的 `debug` 预发布，可由宿主调试仓库直接发现。

### 优化

- 正式 Release 与调试预发布统一生成带通道和提交号的发布元数据，并在完成后事件驱动更新插件索引。

## 1.5.0（2026-07-31）

### 新增特性

- 数据页新增按 UID 保存的 UP / 歪判定来源选择，可独立启停记录原始 `is_up` 字段、社区卡池历史与本地名单推断。
- 内置经锁定和裁剪的 Paimon.moe 原神活动祈愿历史，以及 Star Rail Station 星铁普通、光锥与联动跃迁历史；切换来源后立即重算，不在运行时连接第三方站点。
- 星铁自选可歪角色保留为最高优先级的手动修正规则，与历史来源选择并列展示。

### 优化

- 原神社区历史按账号服务器时区解释版本更新边界；星铁历史按记录中的跃迁 ID 精确匹配，无法确认的五星不再被强行计为 UP 或歪。
- 社区输入记录文件版本、快照时间和 SHA-256，便于审查与后续更新。

## 1.4.0（2026-07-24）

### 新增特性

- 接入官方插件仓库与 GitHub Release 自动发布流程。

### 兼容性

- 插件包升级为 format v2，声明整数版本号、最低宿主版本 11 和插件 SDK 1.1.0。

## 1.3.1（2026-07-16）

### 修复

- 星穹铁道记录统一通过仍在提供 JSON 接口的 `public-operation-hkrpg.mihoyo.com` 读取普通与联动跃迁，避免粘贴旧网关链接后直接返回 HTTP 404。
- 实机确认米游社 SToken 生成的星铁 AuthKey 会被记录接口拒绝后，移除米游社角色列表中的星铁入口并给出 Shizuku、官方云游戏和手动链接指引；原神米游社扫码、加密保持登录和快速生成链接继续可用。
- 千星奇域首次完整读取后会为 UID 和卡池保存“历史已完整”标记；旧版数据仍会自动补齐一次，后续同步则在命中连续本地记录后停止，不再每次扫描全部历史。
- HTTP 错误现在附带不含查询参数的主机和路径，便于定位接口变更且不会泄露 AuthKey。

### 兼容性

- 插件清单新增对宿主内置 `shizuku_auth` 插件的依赖声明，安装、启停和删除时由宿主统一校验依赖关系。

## 1.3.0（2026-07-14）

### 新增特性

- 星穹铁道账号可从本地活动跃迁记录中选择额外的“可歪角色”；选择按 UID 保存在本机，同名五星会参与歪卡数、UP 数、平均 UP 与时间线判定。

### 修复

- 千星奇域颂愿接口改用每页 5 条的分页上限，满页后继续携带最后一条记录 ID 请求下一页；即使旧版本保存的第一页已与本地重复，也会继续翻页并自动补齐后续历史。
- 千星奇域常驻颂愿改为按 4 星、70 抽保底统计当前进度、平均抽数与时间线，不再显示不存在的常驻 5 星。

### 优化

- 卡池分析模型支持按卡池指定目标星级，普通祈愿、活动颂愿与常驻颂愿使用各自正确的统计标签和保底重置条件。

## 1.2.0（2026-07-14）

### 新增特性

- 米游社扫码或 Cookie 登录成功后，使用 Android Keystore AES-GCM 在本机加密保持登录状态；插件重开后可直接刷新绑定角色，也可随时退出并删除凭据。

### 修复

- 修复米游社 `binding/genAuthKey` 生成的星穹铁道 AuthKey 被发送到游戏内记录网关后返回 `-100 authkey error` 的问题；米游社生成链接改用对应记录网关，并补齐官方页面请求上下文。
- 保留手动粘贴、Shizuku 与云游戏捕获链接原有的星铁记录网关，避免影响游戏内 AuthKey。
- 已保存的 CookieToken 失效时会使用加密保存的 SToken 自动刷新并重试角色读取，同时不再对确定的 AuthKey 接口错误做无意义的重复请求。

## 1.1.1（2026-07-14）

### 修复

- 修复米游社扫码确认后直接使用 SToken 查询绑定角色导致 `-10001 invalid request` 的问题；现先换取 CookieToken，再通过 Cookie 角色接口读取绑定角色。
- 同步米游社 2.109.0 请求头及 DS 盐值，并按接口要求分别使用 `account_id + cookie_token` 查询角色、使用 `stoken + mid` 生成 AuthKey。

## 1.1.0（2026-07-14）

### 新增特性

- 米游社账号导入改用 App 扫码登录，直接取得生成 AuthKey 所需的临时 SToken。
- 支持拉取、保存及通过 UIGF v4.2 `hk4e_ugc` 导入导出千星奇域常驻与活动颂愿记录。
- UIGF 导出可选择仅原神、仅星穹铁道或全部游戏。
- 概览新增欧非判定、UP/歪统计、平均 UP 与带颜色等级的五星时间线。

### 优化

- 参考卡池分析时间线重新组织概览，突出当前垫抽、五星抽数、UP 获取成本和歪卡结果。
- 对限定池使用常驻五星名单与本地常驻记录联合推断歪卡，减少新常驻角色遗漏。

### 修复

- 修复当前米游社网页登录仅返回 v2 Cookie 时错误提示缺少 SToken 或 login_ticket、无法继续导入的问题。

## 1.0.1（2026-07-14）

### 修复

- 修复切换本地账号后“读取本地记录”状态不会结束的问题。
- 为账号记录查询增加匹配时间与大整数 ID 排序的数据库索引。

### 优化

- 缓存最近两个账号的记录、卡池统计和保底序号，重复切换不再重新查询和计算。
- 在后台预计算概览统计与单条记录保底序号，减少大记录量下的界面卡顿。

## 1.0.0（2026-07-14）

### 新增特性

- 支持通过 Shizuku 日志、官方云游戏、米游社登录或手动粘贴获取原神与星穹铁道抽卡链接，并可复制当前链接。
- 支持增量拉取各卡池记录并按账号长期保存在本机，不保存 AuthKey、Cookie 或账号密码。
- 提供卡池保底进度、星级分布、平均出金、五星时间线和完整记录筛选。
- 支持 UIGF v4.2 多游戏多账号文件导入、导出和按记录 ID 合并，并兼容 UIGF v3 与 SRGF v1 导入。
