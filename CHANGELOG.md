# 更新日志

## 1.3.1（2026-07-16）

### 修复

- 星穹铁道记录统一通过仍在提供 JSON 接口的 `public-operation-hkrpg.mihoyo.com` 读取普通与联动跃迁，避免粘贴旧网关链接后直接返回 HTTP 404。
- 实机确认米游社 SToken 生成的星铁 AuthKey 会被记录接口拒绝后，移除米游社角色列表中的星铁入口并给出 Shizuku、官方云游戏和手动链接指引；原神米游社扫码、加密保持登录和快速生成链接继续可用。
- 千星奇域首次完整读取后会为 UID 和卡池保存“历史已完整”标记；旧版数据仍会自动补齐一次，后续同步则在命中连续本地记录后停止，不再每次扫描全部历史。
- HTTP 错误现在附带不含查询参数的主机和路径，便于定位接口变更且不会泄露 AuthKey。

### 兼容性

- 插件清单新增对宿主内置 `shizuku_auth` 插件的依赖声明，安装、启停和删除时由宿主统一校验依赖关系。

## 1.3.0（2026-07-14）

### 新增特性

- 星穹铁道账号可从本地活动跃迁记录中选择额外的“可歪角色”；选择按 UID 保存在本机，同名五星会参与歪卡数、UP 数、平均 UP 与时间线判定。

### 修复

- 千星奇域颂愿接口改用每页 5 条的分页上限，满页后继续携带最后一条记录 ID 请求下一页；即使旧版本保存的第一页已与本地重复，也会继续翻页并自动补齐后续历史。
- 千星奇域常驻颂愿改为按 4 星、70 抽保底统计当前进度、平均抽数与时间线，不再显示不存在的常驻 5 星。

### 优化

- 卡池分析模型支持按卡池指定目标星级，普通祈愿、活动颂愿与常驻颂愿使用各自正确的统计标签和保底重置条件。

## 1.2.0（2026-07-14）

### 新增特性

- 米游社扫码或 Cookie 登录成功后，使用 Android Keystore AES-GCM 在本机加密保持登录状态；插件重开后可直接刷新绑定角色，也可随时退出并删除凭据。

### 修复

- 修复米游社 `binding/genAuthKey` 生成的星穹铁道 AuthKey 被发送到游戏内记录网关后返回 `-100 authkey error` 的问题；米游社生成链接改用对应记录网关，并补齐官方页面请求上下文。
- 保留手动粘贴、Shizuku 与云游戏捕获链接原有的星铁记录网关，避免影响游戏内 AuthKey。
- 已保存的 CookieToken 失效时会使用加密保存的 SToken 自动刷新并重试角色读取，同时不再对确定的 AuthKey 接口错误做无意义的重复请求。

## 1.1.1（2026-07-14）

### 修复

- 修复米游社扫码确认后直接使用 SToken 查询绑定角色导致 `-10001 invalid request` 的问题；现先换取 CookieToken，再通过 Cookie 角色接口读取绑定角色。
- 同步米游社 2.109.0 请求头及 DS 盐值，并按接口要求分别使用 `account_id + cookie_token` 查询角色、使用 `stoken + mid` 生成 AuthKey。

## 1.1.0（2026-07-14）

### 新增特性

- 米游社账号导入改用 App 扫码登录，直接取得生成 AuthKey 所需的临时 SToken。
- 支持拉取、保存及通过 UIGF v4.2 `hk4e_ugc` 导入导出千星奇域常驻与活动颂愿记录。
- UIGF 导出可选择仅原神、仅星穹铁道或全部游戏。
- 概览新增欧非判定、UP/歪统计、平均 UP 与带颜色等级的五星时间线。

### 优化

- 参考卡池分析时间线重新组织概览，突出当前垫抽、五星抽数、UP 获取成本和歪卡结果。
- 对限定池使用常驻五星名单与本地常驻记录联合推断歪卡，减少新常驻角色遗漏。

### 修复

- 修复当前米游社网页登录仅返回 v2 Cookie 时错误提示缺少 SToken 或 login_ticket、无法继续导入的问题。

## 1.0.1（2026-07-14）

### 修复

- 修复切换本地账号后“读取本地记录”状态不会结束的问题。
- 为账号记录查询增加匹配时间与大整数 ID 排序的数据库索引。

### 优化

- 缓存最近两个账号的记录、卡池统计和保底序号，重复切换不再重新查询和计算。
- 在后台预计算概览统计与单条记录保底序号，减少大记录量下的界面卡顿。

## 1.0.0（2026-07-14）

### 新增特性

- 支持通过 Shizuku 日志、官方云游戏、米游社登录或手动粘贴获取原神与星穹铁道抽卡链接，并可复制当前链接。
- 支持增量拉取各卡池记录并按账号长期保存在本机，不保存 AuthKey、Cookie 或账号密码。
- 提供卡池保底进度、星级分布、平均出金、五星时间线和完整记录筛选。
- 支持 UIGF v4.2 多游戏多账号文件导入、导出和按记录 ID 合并，并兼容 UIGF v3 与 SRGF v1 导入。
